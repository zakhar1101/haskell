

module Main where


addTo::String->[String]->[String]
addTo newHost hosts=do
    -- print(hosts)
    newHost:hosts


isEven n = if n `mod` 2 == 0 then True else False

-- nop::IO()
-- nop = sequence_ []

show 0=return ()
show n=do
    -- print (if n `mod` 2==0 then n else "None")
    -- s::Int
    -- doif::Bool->IO()->IO()
    -- print(if n `mod` 2==0  then n else pure ())
    -- print(s)
    -- if n%2==0
    --     then print n
    -- isEven n = if n `mod` 2 == 0 then print(n) else false
    show (n-1)





main::IO()
main=do 
    print "sf"
    print("sdf")
    -- print("1.1.1.1" `addTo` hosts)
    --     where hosts=["2.2.2.2", "3.3.3.3"]
    -- print ten where ten=[1..10]
    -- print eng where eng=['a'..'z']
    print([-100,-95..100])
    -- print(["a","aa".."aaaaaaa"])
    func(10)

    -- print even 10
    -- putStrLn eng where eng=['А'..'Я']
    -- print("sdf")
